---
# group_vars/all.yml — Biến dùng chung cho mọi host

project_name: notemesh
app_environment: prod
aws_region: ap-southeast-1
domain_name: catcosy.shop

# Thư mục deploy trên server
app_dir: /opt/notemesh
app_user: ubuntu
